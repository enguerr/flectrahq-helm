apiVersion: v2
name: flectrahq
description: A Helm chart to deploy flectrahq on kubernetes
type: application
version: 0.1.0
appVersion: "1.16.0"
dependencies:
  - name: postgresql
    chart: postgresql
    version: "13.2.16"
    repository: "https://charts.bitnami.com/bitnami"